<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sq.mynotesbag">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NoteViewer"
            android:label="MyNotesBag Viewer" />
        <activity
            android:name=".insertNote"
            android:label="MyNotesBag Editor" />
        <activity
            android:name=".preferences_textsize"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Settings"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".preferences_textsize"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".preferences_textColor" />
        <activity android:name=".preferences_fontType"></activity>
    </application>

</manifest>